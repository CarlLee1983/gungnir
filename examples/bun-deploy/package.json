{
  "name": "bun-deploy-example",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target=bun",
    "start": "bun run dist/index.js",
    "test": "bun test"
  }
}
